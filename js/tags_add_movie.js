var tagGenre = new TagsInput({
    selector: 'movie_genre',
    duplicate : false,
    max : 10
});

tagGenre.addData(["Aventure"])

var tagActeur = new TagsInput({
    selector: 'movie_actor',
    duplicate : false,
    max : 10
});

tagActeur.addData(["Leonardo DiCaprio"])